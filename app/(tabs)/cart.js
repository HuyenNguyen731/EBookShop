import { ScrollView } from "react-native";
import { Cart } from "../../components";

const CartPage = () => {
  return (
    <ScrollView>
      <Cart />
    </ScrollView>
  );
};

export default CartPage;
