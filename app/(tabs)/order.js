import { ScrollView } from "react-native";
import { Order } from "../../components";

const OrderPage = () => {
  return (
    <ScrollView>
      <Order />
    </ScrollView>
  );
};

export default OrderPage;
